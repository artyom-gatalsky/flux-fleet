apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: dashboard
  namespace: kube-system
spec:
  chart:
    spec:
      version: ">=5.8.0"
  values:
    ingress:
      hosts:
        - dashboard.prod
